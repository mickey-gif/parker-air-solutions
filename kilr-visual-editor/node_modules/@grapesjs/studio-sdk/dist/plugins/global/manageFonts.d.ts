import type { Editor } from 'grapesjs';
import { AssetManagerProps } from '../../types';
interface OpenFontAssetManagerProps extends Omit<AssetManagerProps, 'panelId'> {
    /**
     * Callback after adding a font to the project.
     */
    onSelect?: AssetManagerProps['onSelect'];
}
export declare const addFontWithAssetManager: (editor: Editor, { onSelect, ...rest }?: OpenFontAssetManagerProps) => void;
export {};
