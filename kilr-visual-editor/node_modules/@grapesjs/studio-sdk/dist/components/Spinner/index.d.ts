import type { HTMLProps } from 'react';
export interface SpinnerProps extends Omit<HTMLProps<HTMLDivElement>, 'size'> {
    full?: boolean;
    abs?: boolean;
    small?: boolean;
    size?: 'x2s' | 'xs' | 's';
    bg?: boolean;
    currentColor?: boolean;
}
export default function Spinner({ full, abs, small, size, className, bg, currentColor }: SpinnerProps): import("react/jsx-runtime").JSX.Element;
