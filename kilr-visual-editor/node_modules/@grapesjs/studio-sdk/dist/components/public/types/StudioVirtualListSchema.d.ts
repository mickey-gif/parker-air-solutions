import { StudioLayouts } from './index';
import type { StudioLayoutBasePropsConfig } from './StudioBaseSchema';
export interface StudioLayoutVirtualListConfig extends Pick<StudioLayoutBasePropsConfig, 'id' | 'style' | 'className' | 'htmlAttrs'> {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.virtualList}`;
    /**
     * The items to be displayed in the virtual list.
     * @example [{ id: '1', name: 'Item 1' }, { id: '2', name: 'Item 2' }]
     */
    items: Record<string, any>[];
    /**
     * The layout component for the items in the virtual list.
     * @examples ({ item, index, editor }) => ({ type: 'row', children: `${index + 1}. ${item.name}` })
     */
    itemLayout?: '__fn__';
    /**
     * The layout component rendered as footer in the virtual list.
     */
    footerLayout?: '__fn__';
    /**
     * The number of items per row.
     * @default 12
     */
    itemsPerRow?: number;
}
