import React from 'react';
export declare enum SortDirection {
    horizontal = "horizontal",
    vertical = "vertical"
}
interface SortableItem<T = any> {
    id: string;
    entity: T;
}
interface SortableProps<T = any> {
    items: SortableItem<T>[];
    direction: SortDirection;
    onDrop?: (item: SortableItem<T>, newIndex: number) => void;
    renderItem: (item: SortableItem<T>) => React.ReactNode;
}
declare function Sortable<T = any>({ items, direction, onDrop, renderItem }: SortableProps<T>): import("react/jsx-runtime").JSX.Element;
export default Sortable;
