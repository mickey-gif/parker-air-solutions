import { ButtonProps } from '.';
import { TooltipProps } from '../Tooltip';
export declare interface ButtonWithTooltipProps extends ButtonProps {
    classNameWrp?: string;
    tooltipProps?: Partial<Omit<TooltipProps, 'title'> & {
        title?: string;
    }>;
}
export default function ButtonWithTooltip({ children, title, classNameWrp, tooltipProps, ...props }: ButtonWithTooltipProps): import("react/jsx-runtime").JSX.Element;
