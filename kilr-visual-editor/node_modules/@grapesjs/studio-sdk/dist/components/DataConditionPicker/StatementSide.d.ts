import { FC } from 'react';
import { ValueInputRendererProps } from './ValueInputRenderer';
import { ValueType } from './constants';
import { Side } from './statementTypes';
import { AdditionalDataSources } from '../../utils/types';
export interface StatementSideProps extends ValueInputRendererProps {
    label: string;
    onTypeChange: (side: Side, type: ValueType) => void;
    typeOptions: {
        id: string;
        label: string;
    }[];
    additionalSources: AdditionalDataSources;
}
export declare const StatementSide: FC<StatementSideProps>;
